node_modules/
.next/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store


# Convex CLI's auto-installed AI-agent tooling (`npx convex ai-files
# install`, which `npx convex dev` appears to trigger on first run):
# 80+ files of skill docs/configs duplicated under .agents/ and .claude/.
# Generic CLI scaffolding, not path 4-specific and not needed to run the
# backend â€” left untracked rather than bloating the PR.
.agents/
.claude/
AGENTS.md
CLAUDE.md
skills-lock.json

# Convex codegen's optional AI-agent documentation is not runtime output.
convex/_generated/ai/

