# Optional. The app remains fully functional with deterministic fallback copy when unset.
OPENAI_API_KEY=
# Defaults to the low-cost multimodal model below and can be changed without code edits.
OPENAI_MODEL=gpt-5.6-luna
